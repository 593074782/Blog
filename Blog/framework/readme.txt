framework => 框架
这里写：
  	Controller => 控制器 （这里是数据进入口 例：$_GET[''] $_POST[''] 输入口 将接收到的各种数据传给Model）  => 输入
  	Model =>模块 （这是整个操作的核心：写各种逻辑部分,处理Controller传递来的数据，处理后的结果传给View）   => 处理
 	View=>视图（项目的页面：HTML CSS JS 。  接收Model传来的反应结果，并输出到页面上）					   => 输出